// The block palette registry lives in @pagecraft/model (shared with the backend /
// any server-side convert). Re-exported here so existing imports keep working.
export { BLOCKS, BLOCK_ORDER } from "@pagecraft/model";
export type { BlockReg } from "@pagecraft/model";
